node_modules/
.turbo/
dist/
coverage/
.env
.env.*
!.env.example

# Ignore packaged skill zip files (they come from the Agensi dev folder during sync)
skills/**/*.zip
skills/*/*.zip
skills/*/*/*.zip

apps/mcp-server/data/doc-index.json
# Personal/local-only tooling (do not commit)
scripts/local-only/
local-only/
