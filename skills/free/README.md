# Free Agensi Skills

This directory contains the source-of-truth versions of all free Agensi skills.

Each skill is stored in its own folder using the Agensi slug as the directory name.

## Current Free Skills

*(This list will be maintained automatically or via the sync tooling)*

## Usage

These files are intended to be synchronized with the Agensi marketplace using the tooling in `scripts/`.

Do not edit these files directly unless you are preparing an update that will be published via the sync workflow.
