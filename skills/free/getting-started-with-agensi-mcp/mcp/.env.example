# Optional for local-only checks.
# Required (or recommended) for live marketplace features.
AGENSI_API_KEY=

# Optional: override the default Agensi MCP server URL
# AGENSI_MCP_URL=https://mcp.agensi.io/mcp