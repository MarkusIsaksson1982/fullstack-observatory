{
  "name": "@hpc-observatory/hooks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prolog": "python -m hooks.prolog",
    "epilog": "python -m hooks.epilog",
    "test": "pytest tests/"
  },
  "dependencies": {
    "prometheus-client": "^0.19.0",
    "python-dotenv": "^1.0.0"
  },
  "devDependencies": {
    "pytest": "^8.0.0"
  }
}
