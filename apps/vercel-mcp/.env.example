# Guardrails for the hosted Vercel MCP demo (read-only or limited demo mode recommended for public deployment)
VERCEL_ALLOW_DEPLOYS=false
VERCEL_ALLOWED_PROJECTS=

# For a real hosted MCP surface you would normally NOT put a token here.
# This demo is primarily a landing page + future read-only or user-supplied-token surface.
# VERCEL_TOKEN=  (leave empty for public demo)