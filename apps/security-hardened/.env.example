PORT=3000
NODE_ENV=development

# CORS - comma-separated allowlist
CORS_ALLOWLIST=https://your-app.com,http://localhost:3000

# Rate limiting
RATE_LIMIT_MAX=30
RATE_LIMIT_WINDOW_MS=60000

# Auth (demo token from Layer 2)
AUTH_BEARER_TOKEN=demo-token-2026
